# Features

Business modules must be added here as isolated feature folders. Do not place feature implementation directly under page or route folders.

Expected feature structure:

```text
src/features/{feature-name}/
  controllers/
  dtos/
  repositories/
  routes/
  services/
  tests/
  types/
  index.ts
```
