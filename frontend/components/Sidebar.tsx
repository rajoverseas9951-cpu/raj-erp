'use client'; import Link from 'next/link'; import {usePathname,useRouter} from 'next/navigation'; import {clearToken} from '@/lib/api';
const links=[['/','Dashboard'],['/customers','Customer CRM'],['/vehicles','Vehicle Master']]; export default function Sidebar(){const path=usePathname(),router=useRouter();if(path==='/login')return null;return <aside className="fixed inset-y-0 w-64 bg-brand-950 p-6 text-white"><div className="mb-10"><div className="text-2xl font-bold">RAJ ERP</div><div className="text-xs text-blue-200">Operations workspace</div></div><nav className="space-y-2">{links.map(([href,label])=><Link key={href} href={href} className={`block rounded-lg px-4 py-3 ${path===href||href!=='/'&&path.startsWith(href)?'bg-brand-600':'hover:bg-white/10'}`}>{label}</Link>)}</nav><button className="absolute bottom-6 left-6 text-sm text-blue-200 hover:text-white" onClick={()=>{clearToken();router.push('/login')}}>Sign out</button></aside>}
