"use client";
import Link from 'next/link';
import {useEffect,useState} from 'react';
import {useSearchParams} from 'next/navigation';
import {Fleet,fleetApi} from '@/lib/fleets';

export function FleetVehiclePicker(){const search=useSearchParams();const[rows,setRows]=useState<Fleet[]>([]);const[selected,setSelected]=useState('');useEffect(()=>{fleetApi.list().then(setRows).catch(()=>undefined)},[]);useEffect(()=>{const q=search.get('fleet')||'';setSelected(q);document.cookie=`raj_fleet_id=${q}; Path=/; SameSite=Lax${q?'':' ; Max-Age=0'}`;return()=>{}},[search]);function choose(v:string){setSelected(v);document.cookie=v?`raj_fleet_id=${v}; Path=/; SameSite=Lax`:'raj_fleet_id=; Path=/; Max-Age=0; SameSite=Lax'}return <section className="mx-auto mb-4 max-w-[1500px] rounded-[22px] border border-blue-100 bg-white p-4 shadow-sm"><div className="flex flex-wrap items-end gap-3"><div className="min-w-[260px] flex-1"><p className="text-[10px] font-black uppercase tracking-[.18em] text-blue-600">Fleet Mapping · Optional</p><label className="mt-1 block text-sm font-black text-slate-800">Link this vehicle to a fleet / group owner<select value={selected} onChange={e=>choose(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">Individual vehicle — no fleet</option>{rows.map(f=><option key={f.id} value={f.id}>{f.fleet_name} · {f.fleet_code} · {f.vehicle_count} vehicles</option>)}</select></label><p className="mt-2 text-xs text-slate-500">Customer ownership remains unchanged. Fleet is only an additional grouping layer for compliance, reports and outstanding control.</p></div><Link href="/fleets" className="rounded-xl border border-blue-200 bg-blue-50 px-4 py-3 text-sm font-black text-blue-700">Fleet Master</Link></div></section>}