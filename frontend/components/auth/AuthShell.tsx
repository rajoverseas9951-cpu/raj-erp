import Link from 'next/link';
export function AuthShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <main className="auth-shell"><section className="brand-panel"><div className="logo"><span className="logo-mark">R</span> Raj ERP</div><div className="brand-copy"><h1>Run your operations with confidence.</h1><p>One secure workspace for your team, customers and vehicles. Built for reliable day-to-day business.</p></div><small>© {new Date().getFullYear()} Raj ERP · Protected access</small></section><section className="form-panel"><div className="auth-card"><div className="eyebrow">Secure portal</div><h2>{title}</h2><p className="subtitle">{subtitle}</p>{children}</div></section></main>}
export function BackToLogin(){return <Link className="link back" href="/login">← Back to sign in</Link>}
