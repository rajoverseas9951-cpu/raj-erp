export type Customer = { id:string; customer_code:string; first_name:string; middle_name?:string; last_name:string; mobile:string; city?:string; vehicles_count:number; insurance_policies_count:number; rto_files_count:number; gst_number?:string; status:string; priority:string; email?:string; current_address?:string; remarks?:string; tags?:string[] };
const API = process.env.NEXT_PUBLIC_API_URL ?? '';
async function request<T>(path:string, init?:RequestInit): Promise<T> { const res=await fetch(`${API}/api/v1${path}`,{...init,headers:{'Content-Type':'application/json',...(init?.headers||{})},cache:'no-store'}); if(!res.ok) throw new Error(`API request failed: ${res.status}`); const json=await res.json(); return json.data as T; }
export const customerApi={ list:(q='')=>request<{data:Customer[];links:unknown;meta:unknown}>(`/customers${q}`), get:(id:string)=>request<Customer>(`/customers/${id}`), timeline:(id:string)=>request<{data:TimelineEvent[]}>(`/customers/${id}/timeline`), create:(body:unknown)=>request<Customer>('/customers',{method:'POST',body:JSON.stringify(body)}), update:(id:string,body:unknown)=>request<Customer>(`/customers/${id}`,{method:'PUT',body:JSON.stringify(body)}), bulkDelete:(ids:string[])=>request('/customers/bulk-delete',{method:'POST',body:JSON.stringify({ids})}), bulkAssign:(ids:string[],assigned_to:string)=>request('/customers/bulk-assign',{method:'POST',body:JSON.stringify({ids,assigned_to})}) };
export type TimelineEvent={id:string; event_type:string; title:string; description?:string; created_at:string; metadata?:Record<string,unknown>};
