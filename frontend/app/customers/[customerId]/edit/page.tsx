'use client'; import {use,useEffect,useState} from 'react'; import CustomerForm from '@/components/customers/CustomerForm'; import {customerApi,type Customer} from '@/lib/customers'; export default function Edit({params}:{params:Promise<{customerId:string}>}){const {customerId}=use(params),[item,setItem]=useState<Customer>();useEffect(()=>{void customerApi.get(customerId).then(setItem)},[customerId]);return <><h1 className="mb-6 text-3xl font-bold">Edit customer</h1>{item?<CustomerForm customer={item}/>:<p>Loading…</p>}</>}
