import { CustomerTable } from '@/components/customers/CustomerTable'; import { customerApi } from '@/lib/customers';
export default async function CustomersPage({searchParams}:{searchParams:Promise<Record<string,string>>}){ const qs=new URLSearchParams(await searchParams).toString(); const customers=await customerApi.list(qs?`?${qs}`:''); return <main className="space-y-6 p-6"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-bold">Customer CRM</h1><p className="text-slate-500">Enterprise customer master with insurance, RTO, GST and document context.</p></div><a href="/customers/new" className="rounded-md bg-blue-700 px-4 py-2 text-white">Add Customer</a></div><CustomerTable customers={customers.data}/></main> }
