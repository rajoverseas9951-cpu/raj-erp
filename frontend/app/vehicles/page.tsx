import { VehicleTable } from '@/components/vehicles/VehicleTable'; import { vehicleApi } from '@/lib/vehicles';
export default async function VehiclesPage({searchParams}:{searchParams:Promise<Record<string,string>>}){ const qs=new URLSearchParams(await searchParams).toString(); const vehicles=await vehicleApi.list(qs?`?${qs}`:''); return <main className="space-y-6 p-6"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-bold">Vehicle Master</h1><p className="text-slate-500">Customer-owned fleet records with compliance status, documents and timeline visibility.</p></div><a href="/vehicles/new" className="rounded-md bg-blue-700 px-4 py-2 text-white">Add Vehicle</a></div><VehicleTable vehicles={vehicles.data}/></main> }
