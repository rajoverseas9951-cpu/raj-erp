'use client'; import Link from 'next/link'; import {useEffect,useState} from 'react'; import {vehicleApi,type Vehicle} from '@/lib/vehicles'; export default function Vehicles(){const [items,setItems]=useState<Vehicle[]>([]),[error,setError]=useState('');useEffect(()=>{vehicleApi.list().then(x=>setItems(x.data)).catch(x=>setError(String(x)))},[]);return <><header className="mb-8 flex items-end"><div><p className="text-sm font-semibold uppercase text-brand-600">Vehicle Master</p><h1 className="text-3xl font-bold">Vehicles</h1></div><Link href="/vehicles/new" className="ml-auto rounded-lg bg-brand-600 px-4 py-2 text-white">New vehicle</Link></header>{error&&<p className="mb-4 text-red-700">{error}</p>}<div className="overflow-hidden rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr>{['Registration','Vehicle','Fuel','Year','Customer','Status',''].map(x=><th className="p-4" key={x}>{x}</th>)}</tr></thead><tbody>{items.map(v=><tr className="border-t" key={v.id}><td className="p-4 font-semibold">{v.registration_number}</td><td className="p-4">{v.manufacturer} {v.model}</td><td className="p-4 capitalize">{v.fuel_type}</td><td className="p-4">{v.manufacture_year||'—'}</td><td className="p-4">{v.customer?`${v.customer.first_name} ${v.customer.last_name}`:'—'}</td><td className="p-4 capitalize">{v.status}</td><td className="p-4"><Link href={`/vehicles/${v.id}`} className="text-brand-700">View</Link></td></tr>)}</tbody></table>{!items.length&&!error&&<p className="p-8 text-center text-slate-500">No vehicles found.</p>}</div></>}
