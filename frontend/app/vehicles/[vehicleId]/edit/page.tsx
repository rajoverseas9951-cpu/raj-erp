'use client';import {use,useEffect,useState} from 'react';import VehicleForm from '@/components/VehicleForm';import {vehicleApi,type Vehicle} from '@/lib/vehicles';export default function EditVehicle({params}:{params:Promise<{vehicleId:string}>}){const {vehicleId}=use(params),[v,setV]=useState<Vehicle>();useEffect(()=>{void vehicleApi.get(vehicleId).then(setV)},[vehicleId]);return <><h1 className="mb-6 text-3xl font-bold">Edit vehicle</h1>{v?<VehicleForm vehicle={v}/>:<p>Loading…</p>}</>}
