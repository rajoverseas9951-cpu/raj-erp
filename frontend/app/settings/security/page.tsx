"use client";
import { FormEvent, useState } from "react";
import { profileApi } from "@/lib/profile";
export default function SecuritySettings(){const [show,setShow]=useState(false);const [saving,setSaving]=useState(false);const [message,setMessage]=useState("");const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setMessage("");setError("");const form=new FormData(e.currentTarget);try{await profileApi.changePassword({current_password:String(form.get("current_password")),password:String(form.get("password")),password_confirmation:String(form.get("password_confirmation"))});e.currentTarget.reset();setMessage("Password changed. Other active sessions have been revoked.");}catch(x){setError(x instanceof Error?x.message:"Unable to change password.");}finally{setSaving(false)}}return <section className="max-w-2xl rounded-[28px] border bg-white p-5 shadow-sm sm:p-8"><h2 className="text-xl font-black">Change password</h2><p className="mt-1 text-sm text-slate-500">Use at least 10 characters with uppercase, lowercase, a number and a special character.</p>{error&&<div role="alert" className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}{message&&<div role="status" className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{message}</div>}<form onSubmit={submit} className="mt-6 space-y-5">{[["current_password","Current password"],["password","New password"],["password_confirmation","Confirm new password"]].map(([name,label])=><label key={name} className="block"><span className="mb-2 block text-sm font-bold">{label}</span><input name={name} type={show?"text":"password"} required minLength={name==="current_password"?undefined:10} autoComplete={name==="current_password"?"current-password":"new-password"} className="w-full rounded-xl border px-4 py-3"/></label>)}<label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={show} onChange={e=>setShow(e.target.checked)}/> Show passwords</label><button disabled={saving} className="rounded-xl bg-blue-600 px-6 py-3 font-bold text-white disabled:opacity-60">{saving?"Updating…":"Change password"}</button></form></section>}
