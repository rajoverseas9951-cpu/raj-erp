'use client';
import Link from 'next/link'; import {FormEvent,useState} from 'react'; import {AuthShell} from '@/components/auth/AuthShell'; import {authRequest} from '@/lib/auth';
export default function Login(){const[loading,setLoading]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);try{const data=await authRequest<{token:string}>('login',{tenant_id:String(f.get('tenant_id')),email:String(f.get('email')),password:String(f.get('password')),device_name:'web'});sessionStorage.setItem('raj_erp_token',data.token);setSuccess('Signed in successfully. Your secure session is ready.');window.location.href="/dashboard";}catch(x){setError(x instanceof Error?x.message:'Sign in failed.');}finally{setLoading(false)}}return <AuthShell title="Welcome back" subtitle="Sign in with your organization account to continue."><form onSubmit={submit}>{error&&<div className="notice error" role="alert">{error}</div>}{success&&<div className="notice success" role="status">{success}</div>}<label className="field">Organization ID<input name="tenant_id" required placeholder="Your organization UUID" autoComplete="organization"/></label><label className="field">Email address<input name="email" type="email" required autoComplete="email" placeholder="name@company.com"/></label><label className="field">Password<input name="password" type="password" required autoComplete="current-password" placeholder="Enter your password"/></label><div className="row"><span/><Link className="link" href="/forgot-password">Forgot password?</Link></div><button className="primary" disabled={loading}>{loading?'Signing in…':'Sign in securely'}</button></form></AuthShell>}
